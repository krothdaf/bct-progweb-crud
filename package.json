{
  "name": "bct-progweb-crud-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para Frontend React e Backend Node.js",
  "scripts": {
    "vercel-build": "npm install --prefix ./backend && npm install --prefix ./frontend && npm run build --prefix ./frontend"
  }
}