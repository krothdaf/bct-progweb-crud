{
  "version": 2,
  "public": true,
  
  "installCommand": "npm install --prefix ./frontend && npm run build --prefix ./frontend",
  
  "routes": [
    {
      "src": "/(.*)",
      "dest": "frontend/build/$1"
    }
  ]
}